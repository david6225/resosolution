$('#contactForm').submit(function(e){
/*
	  	e.preventDefault();
	  	url = $form.attr( 'action' );
	  	// Récupérer toutes les valeurs du form et les ajouter à l'objet potData
	  	$.post( url, postData, function(data){
	  		// On success info utilisateur
	  	} )
*/
	  })

!function ($) {
	$(function(){
	  // carousel demo
	  $('#myCarousel').carousel({
	    interval: 8000
	  })

	  
	})
}(window.jQuery)